# Pearagon Task 1

Minimal TypeScript CLI scaffold for Task 1.

## Scripts

- `npm run build` compiles TypeScript into `dist/`
- `npm run start` builds and runs the CLI
- `npm run dev` runs the CLI directly with `tsx`

## Current behavior

Running the CLI prints a setup verification message only. It does not accept input, call an API, or use caching yet.
